Run the notebook `run_experiments.ipynb` from top to bottom to reproduce the results in my report.

Python libraries used: NumPy, sklearn and Product from itertools

The decision tree class is implemented in `decision_tree.py` and the random forest class is implemented in `random_forest.py`.